/**
 * Flipper setup
 * Keep development tooling optional to avoid runtime errors on release builds.
 */

export const initializeFlipper = (): void => {
  console.info('[Flipper] Flipper not configured yet.');
};

initializeFlipper();

